# Render Blueprint - https://render.com/docs/blueprint-spec
services:
  - type: web
    name: therapy-billing-app
    runtime: node
    region: oregon
    plan: free
    buildCommand: npm install --include=dev && NODE_OPTIONS="--max-old-space-size=512" npm run build
    startCommand: node dist/index.js
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: SESSION_SECRET
        sync: false
      - key: PHI_ENCRYPTION_KEY
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: SMTP_PORT
        value: "587"
