# Nixpacks configuration for Railway deployment
# Ensures proper Node.js build with production optimizations

[variables]
NODE_ENV = "production"

[phases.build]
cmds = [
  "npm ci",
  "npm run build"
]

[start]
cmd = "node dist/index.js"
